{
  "argv": [
    "/usr/bin/env",
    "node",
    "${CLODEX_BIN}/clodex-run-tests.js"
  ],
  "cwd": "${TEAM_ROOT}",
  "description": "Suite digest, one line: runs ${TEAM_ROOT}/scripts/run-tests.js, the merge gate's runner. Measures the TEAM ROOT unless `tree` names a worktree of this repo — pass it or you measure master.",
  "timeoutMs": 420000,
  "maxBytes": 1024,
  "replyStderr": true,
  "schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "tree": { "type": "string", "minLength": 1, "maxLength": 1000 }
    }
  }
}
