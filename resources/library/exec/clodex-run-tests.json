{
  "argv": [
    "/usr/bin/env",
    "node",
    "${CLODEX_BIN}/clodex-run-tests.js"
  ],
  "cwd": "${TEAM_ROOT}",
  "description": "Suite digest: runs scripts/run-tests.js. Measures the TEAM ROOT unless `tree` names a worktree. scope: own = branch tests + repo-wide shape checks, no lock; full (default) = the merge gate's run.",
  "timeoutMs": 420000,
  "maxBytes": 1024,
  "replyStderr": true,
  "schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "tree": { "type": "string", "minLength": 1, "maxLength": 1000 },
      "scope": { "type": "string", "enum": ["full", "own"] }
    }
  }
}
