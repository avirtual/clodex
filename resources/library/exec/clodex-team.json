{
  "argv": ["/usr/bin/env", "node", "${CLODEX_BIN}/clodex-team.js"],
  "description": "Your team: roster (who is live, replies), tickets, or retire a teammate. `agent` is your OWN name.",
  "timeoutMs": 10000,
  "maxBytes": 4096,
  "replyStderr": true,
  "schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["action", "agent"],
    "properties": {
      "action": { "type": "string", "enum": ["roster", "retire", "tickets"] },
      "agent": { "type": "string", "maxLength": 64 },
      "target": { "type": "string", "maxLength": 64 },
      "cwd": { "type": "string", "maxLength": 1000 }
    }
  }
}
