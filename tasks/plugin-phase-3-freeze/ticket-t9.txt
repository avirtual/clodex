From: clodex
Time: 14:14:13
Size: 2799 bytes

[ticket t9] tasks/plugin-phase-3-freeze — lint hardening: make the no-backdoor test fail for classes it cannot currently see

Third and lowest-priority of the review findings. Do this LAST, after the plan corrections and the boundary enforcement ticket. Queued now so it isn't lost.

**Frame this correctly before you start.** `test/plugin-boundary.test.js` is NOT a security control and must never be described as one. With `contextIsolation: false` a determined plugin reaches everything, and the published contract already says so: in-process, no sandbox, the host API is a contract not a containment boundary. What this lint does is catch **accidents and drift** — a plugin author reaching for something they shouldn't, in the obvious way. Harden it to that standard and do not let any comment or doc line imply more.

The blind spots the cold review found, all verified as real:
- `:60` sees only string-literal specifiers. `require(k)`, `require('..'+'/x')`, `process.mainModule.require(...)` produce zero specs and pass silently.
- `:46-49` and `:75` allow every node builtin — including `module`. `require('module').createRequire(__filename)('/abs/session-manager.js')` reaches every live core singleton, and neither guard can see it.
- `:90` matches the literal string `window.api`. `globalThis.api`, `window['api']`, `self.api`, and `const w = window; w.api` all pass.

Fixes:
(a) Denylist `module`/`node:module`/`vm`/`process.binding`/`process.mainModule` inside the builtin allowance.
(b) Flag any `require\s*\(` not immediately followed by a quote as "dynamic require — unauditable". A plugin has no legitimate need for one; if the pilot trips this, tell me rather than widening the rule.
(c) Replace the `window.api` regex with a check for `api` reached off `window|globalThis|self|top|parent` in any form, plus a bare `\bapi\s*\.` free-identifier check.

**The requirement that matters most, and the reason this ticket exists at all:** the current self-test at `:147` asserts EXACTLY THREE violations for its synthetic plugin. That number pins the discriminations the lint already makes, so it stays green no matter how many classes it cannot see — a green test measuring its own assumptions. Same shape as the fake-store tests that let the persistence bug through.

So: every new discrimination ships with its own fixture that FAILS before your change and passes after. Restructure the self-test so it asserts *which* violations were found by kind, not how many. An exact-count assertion is the thing being removed here, not a thing to update.

Then re-run the lint against the real pilot and report anything it now flags. A true positive in shipped code is a finding I want; do not silence it.

Commit on plugin-phase-1, do not touch master, do not push. Suite digest in your close.