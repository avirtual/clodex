From: clodex
Time: 13:43:47
Size: 4106 bytes

[ticket t6] tasks/plugin-phase-3-freeze — T6: freeze hostApi to "1" and publish docs/plugin-api.md

CONTEXT. Same worktree, `plugin-phase-1`, HEAD `88a811f`, suite baseline **2459/2459**. Master untouched, nothing pushed, keep it that way. Phase 2 is done: all five manual gates passed on Bogdan's machine, plus T5's step 9.

This is plan §6 Phase 3, minus the acceptance build — that goes to a different agent who will work from your docs and nothing else. Assume your reader cannot read core. That is not a formality: the Phase 3 gate is a plugin built out of tree with **zero core edits**, and every fact you leave out of the docs is a fact that agent will have to guess or fail on.

WHAT TO DO.

1. **Resolve pilot-driven shape changes, then bump `HOST_API_VERSION` from `0` to `"1"`.** Update the workbench manifest and every test that asserts the version. Before you bump, do a deliberate pass over the host surface asking what the pilot proved WRONG or awkward, and propose changes — this is the last moment the shape is free. Two I already know of, both mine to hand you rather than yours to discover:

   - **Slot ordering is undefined.** Registration order is discovery order is `readdirSync` order. With one plugin it's invisible; with two footer buttons it's arbitrary and unstable, and neither a user nor a plugin can influence it. Do NOT invent a priority field speculatively. Document the current behaviour honestly as "unspecified, do not depend on it" so a v1.1 addition isn't a breaking change.
   - **There is no menu slot.** A plugin cannot contribute a system-menu item. T5 wired `getPluginHost` into `app-menus.js`, which is exactly the plumbing such a slot would need, so this is now a real candidate rather than a blocked idea. Document it as a known gap with that note. Don't build it.

2. **Write `docs/plugin-api.md`** — the published, versioned contract. This is the one-way door; it is the deliverable, not a summary of one. It must be complete enough that someone builds a working plugin from it alone. At minimum: the manifest schema field by field with every validation refusal; the engine `host` object; the renderer `rhost` object; all seven UI slots with exact spec shapes; the five-row `plugin:*` transport; the enable/disable and quarantine lifecycle; per-window vs per-app-run (§3.3's laws — the single most likely thing for an outsider to get wrong); what is deliberately NOT exposed and why. Write the refusals as prose an outsider can act on, not as a list of error strings.

3. **A contract-shape test pinning the host surface** — an api-contract-style table for `host` and `rhost` themselves, so the published shape can't drift silently. Same spirit as `api-contract.test.js`.

4. Cross-check `docs/plugin-plan.md` §3.2 against what you actually shipped. The plan was written before the pilot; where they disagree, the CODE is right and the doc is stale. Note the divergences in your report — I want to see them, they're the pilot's real findings.

ADJUDICATIONS from your T5 report, so you can stop hedging on them:
- **The quarantine checkbox stands.** Your reasoning was right; no Retry menu item.
- **Deviation (q), the 221st row, is accepted.** To be explicit for the docs: §1's freeze applies to the **plugin transport** — the five `plugin:*` rows — not to core's total row count. 235→220 was evidence that moving fourteen data methods out shrank core's contract, never a ceiling to defend. Core may add its own chrome. Say this in `plugin-api.md` so nobody re-litigates it.
- Your `announceState` refresh seam and the `renderer/web/menubar.js` catch were both better than my spec. Noted, keep going.

VERIFICATION. Full suite green from the worktree (`node --test`, redirect, read the digest — `clodex-run-tests` is blind to worktrees and will lie). The version bump will break tests that pin `0`; fix them, and flag any where the pin was load-bearing rather than incidental.

Journal as you go. Flag deviations — you've overruled me correctly three times now (leak-scanner, refresh seam, web menubar) and I'd rather have a fourth than a silent compromise.