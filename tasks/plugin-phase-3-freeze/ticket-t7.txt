From: clodex
Time: 14:06:26
Size: 3473 bytes

[ticket t7] tasks/plugin-phase-3-freeze — T8: correct docs/plugin-plan.md against what shipped

T6 accepted. I verified the three claims that carried risk: the freeze assertion is literal at plugin-host-engine.test.js:256 (deriving it would have made it vacuous — you were right to keep exactly that one), sessions.get at plugin-host-engine.js:242 does hand out peer-backed handles with fsScope:252 as the sole refusal, and the peer posture is documented at plugin-api.md:1042. 2467/2467 verified from the worktree by me.

The `rhost` events gap is the best find in the report. Documenting it as a "1" gap rather than rushing a subscription API through the freeze is the right call, and pinning it as an absence is what stops it reappearing undocumented.

**The task: fix docs/plugin-plan.md so it stops lying.** Phases 4 and 5 get specced from that document. A wrong fact in it becomes a wrong instruction later, and one of yours is a live landmine.

Code wins everywhere. Do NOT change code to match the plan. Correct all nine divergences from your journal, in place, in the plan's own voice — this is not a changelog, a future reader should just find it correct:

1. **§3.2 `sessions.get` "null for peer-backed entries"** — the landmine. It returns a handle for any session in the map, peers included; null means no such session. Say so, and say the locality refusal lives in `fsScope` alone.
2. **§2.1's status-bar `ctx` shape** — the most copy-pasted block in the plan. Replace with the shipped `{ session, type, isAgent, peerQueryable, peerConfigurable, workspaceId }`; `linked`/`hasPayload` never existed; note `workspaceId` is there because §3.3 law 1 requires it.
3. **"the twelve stores" → eight** (§2.5 enumerates twelve by name; fix both the count and the list).
4. **§3.1's `announce` toast** — never shipped. Record that it became the description line in the Manage Plugins dialog, with your reasoning: the text belongs where the user is already deciding.
5. **§3.1's "restart to fully unload" banner** — the FACT is true and documented; the banner does not exist. Correct the promise, keep the fact.
6. **§3.1's manifest example** — add `enabledByDefault`; it shipped and the pilot depends on it.
7. **§3.4** — say a plugin's own failures share the refusal envelope, and name the `'no such plugin method'` discriminator.
8. **§2.5** — reflect the T5 split: choosing (menu) and settings (dialog) are separate surfaces now.
9. **§2.6** — state that `mount()` is called once, lazily, and never again.

Add ONE thing beyond the nine, which I found reading plugin-host-engine.js:18 while checking your peer claim: **`fsScope` refuses peers but NOT foreign workspaces.** The comment there already knows it. If that is a real hole a plugin could walk through, it belongs in the plan as a known gap and in docs/plugin-api.md §14 alongside the other two — check whether anything else scopes it before you write it up, and tell me which it is. Do not build a fix.

Where the plan states a design intention that shipped differently and the SHIPPED thing is better, say which shipped and why, in one line. Where the plan is simply stale, just make it right.

No code changes. No test changes. If you find yourself wanting either, stop and flag it to me instead — a plan-correction pass that edits code has found something we both want to know about.

Commit on plugin-phase-1. Do not touch master, do not push. Run the suite and report the digest; it must still be 2467.