---
id: mem-1784673186252-k5ca21
scope: clodexctl
learned_at: 2026-07-21T22:33:06.252Z
source: clodex
tags: release-log,connection-unification
tags_v: 1
pinned: true
---

CLODEX RELEASE — READY TO SHIP, awaiting Bogdan's explicit GO (feature lane: nothing commits/ships without it). Read .claude/memory.md first (live thread). State as of 2026-07-22 clean-boundary reload:

WHAT'S DONE: The whole clodexctl client (kubectl-for-Clodex CLI at cli/) is code-complete, cold-reviewed, and lead-verified. Suite 1886/1886 from repo ROOT (`npm test`=node --test; NEVER from cli/). Once reloaded as Fable, the test-green skill/agent is available — use it for suite verification instead of manual npm test (I'd been testing by hand because the skill wasn't loaded). All task specs stamped ACCEPTED in ~/.clodex/teams/clodex/tasks/ (36-*, 36g-transports, 37-attach, 39-deploy-ssm). Centerpiece = T37 `attach` (live TTY over any transport, self-reconnecting) + `logs -f`. T36g = typed cloud transports (ssm/--ssm-ecs/kubectl/gcloud-iap/az-bastion as shareable DATA over one tunnel mechanism). T39 = `deploy ssm` OS-flavor (root RunCommand wrapper, byte-pinned installer, systemd drop-in token injection; a real root-injection MUST-FIX was caught+fixed via REF_RE validation + random-nonce heredoc delimiters).

VERIFIED LIVE: isolated-engine E2E smoke (attach/detach/read-only/reconnect via TCP-proxy drop) + REAL AWS smoke against OpsGuru instance i-083ce687a7eaf3105 (acct 250121276249, us-west-2, profile opsguru-sso) — spawn/run/attach-with-typing through a real SSM tunnel all passed.

THE UNCOMMITTED BUNDLE (git-staged, nothing committed): cli/ (complete client) + docs/ design docs (engine-as-substrate, deployment-plan, client-story) + docs/recipes/ (aws-fargate, aws-ec2, kubernetes) + .gitignore !cli/deploy/ negation + package.json/lock (clodexctl in devDependencies). Engine files UNTOUCHED — verify with git diff before any commit (house rule: lead reads full bundle diff before commit).

NEXT ACTIONS on reload: (1) re-send the RELEASE ASK to Bogdan via notify-user; (2) he may want a coordinated LIVE `deploy ssm` run onto opsguru-ec2 first (would replace the manual docker container there with the OS-flavor node = first real exercise of the T39 verb — box is hands-off without his go; wire token in scratchpad/ec2-wire-token); (3) on his GO: commit train + scripts/release.sh <minor>.

POST-RELEASE BACKLOG (journaled, non-blocking): T38 /api/task formal completion event; engine peer-client.js attach staleness watchdog (GUI freeze bug — clodexctl sse-guard is the reference); GUI nit batch (parked-msg tooltip, inbox horizontal resize, fileview-Open closes popover, codex-warmth-display — check dm parking also keys on warmth for codex); T36g/T37/T39 minor nits. OpsGuru/Simon wants Clodex as official engagement tool — the typed shareable transports + SSM deploy are the engagement-facing features.
