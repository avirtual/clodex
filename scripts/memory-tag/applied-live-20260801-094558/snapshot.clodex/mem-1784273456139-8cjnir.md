---
id: mem-1784273456139-8cjnir
scope: clodex
learned_at: 2026-07-17T07:30:56.139Z
source: clodex
tags: claude-cli-internals,context-economics
tags_v: 1
pinned: true
---

WAKE-STATE CORRECTION (07-18, supersedes the roster-slimming numbers in the rev-2 WAKE-STATE unit): the morning tool-frequency data was UNDERCOUNTED across the board — wirescope's SSE-tool_use recount (vendored+logs_main, 37.5d) is authoritative and it INVERTS the roster plan. Of the 3 morning deny candidates, ONLY EndConversation was genuinely dead (0 calls) and it's SHIPPED (0da0036, default-on strip). The other two are KILLED: (1) TaskGet — do NOT strip; it's the sole reader of a task's `description` field (TaskList is summary-only, id/subject/status/owner/blockedBy), and core is the HEAVIEST task user in the fleet (clodex-hand 540 Task calls / clodex 184 / wirescope 101; fan-out barely touches tasks). KEEP the whole task subsystem in core. (2) Monitor — do NOT deny; 66 calls/29 sess (core 12, fan-out 54), used in both groups, near-zero in neither. KEEP everywhere. So there are NO core deny toggles: the morning "tick Monitor+TaskGet off for clodex+clodex-hand" step is CANCELLED. KEEP Skill too. Real savings collapses from ~4.7k/turn to just EndConversation's ~1.7k. To make the strip live: relaunch FROM SOURCE (0da0036 is post-v2.33.2; packaged DMG needs a patch cut) then [agent:context clear] — NO tool-gating toggles. Only-remaining (optional, Bogdan's call, not urgent): deny tasks-minus-Stop + Monitor in FAN-OUT/auto-* worker roles where genuinely near-zero, per-role, never clodex/clodex-hand. LESSON: don't trust "seldom used" hunches about tool frequency without the SSE-tool_use count — the /context-style or narrow-window frequency data undercounts; two of three morning denies were load-bearing.
