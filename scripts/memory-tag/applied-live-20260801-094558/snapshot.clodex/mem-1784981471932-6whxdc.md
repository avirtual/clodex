---
id: mem-1784981471932-6whxdc
scope: clodex-plugins
learned_at: 2026-07-25T12:11:11.932Z
source: clodex
tags: plugin-architecture
tags_v: 1
pinned: true
---

PHASE 3 COMPLETE. Branch plugin-phase-1 @ b70917a, 2489/2489, tree clean, 27 commits ahead, master untouched at 2f3f8e1, NOTHING PUSHED. Merge is Bogdan's.
Delivered: hostApi frozen at "1"; docs/plugin-api.md published (~1100 lines); test/plugin-surface-contract.test.js pins shape with literal tables + 3 absences; four boundary invariants enforced (F1 plugin-verb strip at 4 sites, F2 host.lib bound facade, F4 'enabled' reserved, plus double-activation no-op + telemetry deep copy); lint hardened; git-branches installed and committed.
THE VERDICT: a cold agent with ONLY the published doc built a working two-half plugin with an intent verb, ZERO core edits. Contract holds.
ONLY OPEN ITEM: plugins/git-branches/VERIFY.md run against Bogdan's live app — 5 behaviours no harness reaches; 2 unknown (does `cls` arrive prefixed, are plugin stylesheets scoped). Failures there = doc findings, not plugin bugs.
THE LESSON, which outlives the phase: every significant defect was A CLAIM WRITTEN WHERE NO EXECUTION PASSES THROUGH — a comment, a plan marker, a doc line, a ticket instruction. Tests caught NONE of them because tests check code. What caught them: a user running the app; a cold agent building from docs; a hand verifying instructions against source before obeying (it found 4 of MINE wrong — store count, gitWorktree members, a regex blind to its own example, the onCreate reasoning); and revert proofs actually RUN rather than reasoned about (the hand's own first revert proof was worthless — its fixture sat after the test in file order, so it could not fail).
COROLLARY for future me: when I assert an invariant in a comment, either enforce it in code or delete it. plugin-host-engine.js:265 claimed "a careless plugin CANNOT widen locality" and then contradicted itself FOUR LINES LATER; it read as elaboration and survived four reviews.
