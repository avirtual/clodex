---
id: mem-1783461887570-hwg47v
scope: clodex
learned_at: 2026-07-07T22:04:47.570Z
source: clodex
tags: engine-internals
tags_v: 1
---

Build/runtime gotchas beyond CLAUDE.md: node-pty spawn-helper exec bit — npm strips to 0644, afterPack chmods 0755 BEFORE codesign (shipped broken through v0.6.1). npmRebuild:false — node-pty 1.x NAPI prebuilds are ABI-stable; the rebuild pass fails on Python 3.12+, don't re-enable. GUI launch PATH: Dock apps inherit launchd's minimal PATH; main.js runs $SHELL -ilc PACKAGED ONLY — never relaunch a dev GUI via open/launchd, PTY spawns lose claude/codex. Env sanitize: main.js boot strips CLAUDE_*/CLAUDECODE always + ANTHROPIC_BASE_URL when it's an /agent/ proxy route — this is what makes app.relaunch() safe (nested-child transcript death otherwise); do NOT remove. Electron ships BoringSSL (no blake2): wire/ crypto must exist in both runtimes, use sha512-truncated, validate once under ELECTRON_RUN_AS_NODE=1. JsonlWatcher seeks to EOF on first open — byte 0 would re-fire all historical intents on every restart. Task lists are process-lifetime (~/.claude/tasks/session-<id>, id rotates per launch) — durable record is git + plan docs. Fable-5 refusal classifier: trigger is the fresh-BOOT context (dense wire-capture/telemetry language reads as model-extraction, category reasoning_extraction), fable-endpoint only; fallback is a DOUBLE downgrade fable-5[1m] → opus-4-8 200k; a background agent WEDGES silently on the interactive pause menu. Mitigation = lean boot context (the 2026-07-09 memory restructure); detection is free on the wire (stop_reason "refusal"); PTY auto-retry is dead, don't re-propose.
