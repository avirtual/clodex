---
id: mem-1784272812827-g5qf2k
scope: clodex
learned_at: 2026-07-17T07:20:12.827Z
source: clodex
tags: intent-grammar,claude-cli-internals
tags_v: 1
---

SUBAGENT-MESSAGING + HOOK-MECHANICS arc (07-18 morning, DESIGNED+SIZED+PROBE-VERIFIED, build post-relaunch). Origin: Bogdan asked whether the pending-DM drain hook conflicts with subagent tool calls. FINDINGS (all probe-verified on our live CLI via headless haiku spawns + transcript grep, NOT just doc-claimed): (1) BUG FOUND+FIXED: a subagent's tool calls fire the PARENT session's PostToolUse hooks; additionalContext returned there routes into the SUBAGENT context (lost on exit). Our consuming pending-drain (cli-hooks.js, registered UPS+PostToolUse) had no guard → parked DMs stolen by subagent fires, no bounce. GUARD SHIPPED 7f531b5 (hand): drain bails `sys.exit(0)` before the rename-claim when hook input carries `agent_id` (present only on subagent fires, never main). Deferred not lost — drains next main-context event. suite 1270/1270. (2) SIZING of whether to unify the SendMessage-vs-dm address space (wirescope 4-round census, vendored+logs_main 37.5d): peer-SendMessage FAILURES=22 total, self-healing (7/7→dm fallback), ~tens/mo — NOT thousands; DM VOLUME=1,243 intents (1,083 peer-directed, from RESPONSE-SSE recount after I caught the request-side join undercounted); LEGIT SendMessage LOAD=129 (bucket a owner→own-subagent=68/17sess, b sub→parent=61 all team-lead, c protocol-frames=0 unexercised). VERDICT: SendMessage is load-bearing subagent infra (~6× real use over misuse), bucket(a) has no other mechanism → design is ADD a dm-to-subagent+peer address space BESIDE a tool that stays; only has to absorb the 22 peer misfires, never replicate (a)/(b). Rate: 10% of peer-reaching sessions fumble once (wirescope closing a combined-denominator recheck). (3) HOOK MECHANICS probe-verified for the spec anchors: SubagentStart FIRES, its additionalContext LANDS IN THE SUBAGENT (transcript hook_additional_context attachment, grep-confirmed) → clean once-at-boot inbox delivery; SubagentStop FIRES with agent_transcript_path + last_assistant_message → natural exit-bounce trigger; Stop carries last_assistant_message (intent capture w/o symlink poll); SessionEnd carries reason=clear|resume. Named spawns: name is in the parent's Task tool_input.name + tool_response.agentId (background spawns register at async_launched, before first sub tool call) — the name→id join for addressing; sub's OWN fires carry only agent_id+agent_type, never the name. DESIGN (supersedes morning PostToolUse-only sketch): queues at pending/<owner>/sub/<agent_id>/ (glob skips `sub`, backward-compat w/ shipped drain); deliver via SubagentStart inbox + PostToolUse mid-loop; bounce undrained on SubagentStop; roster name→id from Task PTU; ownership gate (owner+operator park freely, third party bounces "dm <owner> to relay"); wrap delivery in an envelope (subagents inherit no IPC prompt). (4) ADJACENT hook leads from ~/projects/ai/claude-code-research/hooks.md (OLD 2026-05 source, treat unprobed as leads): asyncRewake (async hook, exit-2 re-wakes model later) = native path for the parked [agent:monitor] watcher, promote to probe-then-spec; PreToolUse permissionDecision:deny = the parked Bash(true) guard. cli-hooks.js currently wires NONE of SubagentStart/Stop/SessionEnd — all available. Probe rig lives in scratch hookprobe/. NOT URGENT: additive, self-healing footgun; build after the relaunch/clear.
