---
id: mem-1785112615750-0ohztw
scope: clodex
learned_at: 2026-07-27T00:36:55.750Z
source: clodex
tags: release-log
tags_v: 1
pinned: true
---

v4.4.0 SHIPPED 2026-07-27, artifact-verified. Tag https://github.com/avirtual/clodex/releases/tag/v4.4.0, image + latest on ONE digest sha256:6d66b19e0114d0c3882ed6c2926bea8162b820381e052b326862684e80e2d54a (arm64 + amd64). DMG asar verified: peering 4, docs 19, cli 66, plugins 16, wire 10, cli/src/dial.js present. Suite 2759/0, ESCAPES 0.

CONTENTS — four merges, a coherent reliability set:
- t41 peer-client SSE staleness watchdog (a peer that went quiet looked online indefinitely; reconnected on close/error but NOT on silence, not even a missed 25s heartbeat). Reuses cli/src/sse-guard's makeWatchdog + STALE_MS rather than porting, so the 60s/25s relationship cannot drift from the server.
- t42 L1 one dial — three copies of spawn-and-kill collapsed into cli/src/dial.js, leaf direction preserved. Net +110 lines: single-sourcing, NOT a line win, and the hand said so rather than dressing it up. Existing tunnel tests passed UNEDITED (45/45 GUI, 83/83 CLI).
- t43 ssh keepalive — cli/src/transport.js sshArgv was the ONLY ssh call site in the repo without ServerAliveInterval/CountMax while its comment claimed parity. Plus an invariant test that discovers ssh call sites from tracked source via git ls-files, so a 4th site added later fails it.
- t45 strict-mcp-config observability — flag + IPC log line now derive from ONE value (proxy-util.strictMcpReason); null = wire strips = no flag, no line. Hint text corrected: it had told ROUTED users "nothing to do here", resting on "strip-capable", a distinction the user cannot observe.
- plus a packaging-guard fix (layer 3 went red on any developer's gitignored dirs; now filters through git check-ignore).

THE ARGUMENT THIS RELEASE MAKES, worth keeping: t41 and t43 are THE SAME BUG AT DIFFERENT LAYERS (a connection that stops carrying data without closing). Both survived because the connect-to-a-remote-Clodex code was written twice — each side got fixed once, separately, and neither fix crossed over. That is a better argument for unification than any line count: the duplication was not costing size, it was costing FIXES THAT ONLY LANDED ON ONE SIDE.
