---
id: mem-1783728683312-54wqvu
scope: 
learned_at: 2026-07-11T00:11:23.312Z
source: clodex
tags: exec-intent
tags_v: 1
---

Clodex [agent:exec] gotchas (shipped 07-13, commits 6a01a56/a699fbf/f38447f): (1) persistence._load() rereads sessions.json from disk on EVERY call — no cache; atomic external edits to grants take effect immediately in a running app (I once claimed otherwise; wrong). (2) styles.css has NO generic .hidden rule — every drawer/editor id must join the grouped id-scoped selectors (e.g. #agents-drawer,#skills-drawer,...) or it renders always-visible; this bit the exec drawer in review. (3) Exec body capture terminates at first JSON.parse success (f38447f) — trailing prose on FOLLOWING lines is fine, but prose on the SAME line as the JSON value still bounces (unextractable without a lexer); seat prompts keep "exec line last" as belt. (4) The @-pointer spill form ("attached: @path") expands ONLY via PTY stdin; hook-drained additionalContext must inline/reword it (912ab04 does, in generated claude pending.sh only). (5) Registry + grants are operator-only by construction: ipcMain.handle only, no exec-write intent verb — never add one.
