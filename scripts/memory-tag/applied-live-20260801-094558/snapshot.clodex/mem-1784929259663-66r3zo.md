---
id: mem-1784929259663-66r3zo
scope: daemon-direction
learned_at: 2026-07-24T21:40:59.663Z
source: clodex
tags: gorge-once-pattern,claude-cli-internals
tags_v: 1
---

MECHANISM CONFIRMED (2026-07-25, via claude-code source trace of ~6mo-old CLI): the "brain agent" / single-shot synthesis pattern is a TODAY-THING, no brick to build. Feeding a large curated corpus (~600k) to a 1M model in ONE shot: use `--append-system-prompt-file <path>` — the system-prompt file path is UNGATED. Chain: main.tsx:1372 bare readFileSync into string (no stat/size/slice/truncation); systemPrompt.ts:115-122 buildEffectiveSystemPrompt pure concat (no length/token check); api.ts:415-433 splits into blocks tagged cacheScope:'org' → prompt-cached (billed full input rate ONCE, cache-hit later turns), no size gate. print.ts:4369 routes system prompt through STDIN with literal comment "to avoid ARG_MAX limits" — engineered around OS arg ceiling on purpose. Only ceiling = model context window (must be 1M variant). DECOY RULED OUT: @-mentions and Read are NOT bulk loaders — files >256KB silently DROPPED (return null), >25k tokens TRUNCATED to first 2000 lines (attachments.ts:1894/3020, MAX_OUTPUT_SIZE=256KB file.ts:48, DEFAULT_MAX_OUTPUT_TOKENS=25000 limits.ts:18) — would silently feed a truncated corpus. CAVEAT (behavior not limit): system-prompt content framed as INSTRUCTIONS not data → wrap corpus in <context>…</context>, label as reference material, or the model reads it as directives. RECIPE: claude -p "Solve X, write plan.md. Think hard." --append-system-prompt-file ./corpus.md --model claude-fable-5[1m]. Cost model: 1 cache-write (~1.25x) then cache-reads (~0.1x) — "single-shot" tolerates 2-3 finishing turns cheaply if corpus sits at a stable cache breakpoint before variable content. USE FOR: cross-cutting codebase-wide synthesis too big to chunk without losing joins (e.g. the plugin-architecture plan from docs/plugin-vision.md). Curation of WHAT goes in the 600k is now the only hard part; transport is solved.
