---
id: mem-1783989302966-iwij80
scope: peer-statusbar
learned_at: 2026-07-14T00:35:02.966Z
source: clodex
tags: connection-unification,ui-conventions
tags_v: 1
---

DEFERRED (Jul-14, Bogdan's call) — extend the peer-viewer statusbar to expose keep-warm + strip (and the ⚙ session dropdown) over the wire. Framing that settled it: these are effectively SESSION SETTINGS, so route them through the EXISTING `args` cap (the same path Edit Session/Skills already use remotely — remote-wiring.js `/api/session-args` + `/api/session-skills`), NOT new mutating wirescope endpoints. That keeps it inside the one-directional peering trust boundary (docs/peering.md invariant: box→consumer is telemetry+outbox only; args cap is already an allowlisted reach-back). Context: today we confirmed the peer statusbar correctly degrades — ctx/cost charts + files button cross the wire (info-only telemetry view, owner ships no base/capabilities/sessionId per peers-ui.js:851), but cache-bust popover, wirescope link, keep-warm, strip, and the ⚙ session dropdown are owner-local and drop on a peer tab. renderer.js:1414 gates ⚙ session to local claude/codex only; renderer.js:1435 activePeerQueryable() is the files-only peer branch. Cheap in-model pre-win also available (#1): make ctx/bust chips clickable remotely by having the owner advertise `bust`/`ctx` in the payload `queries` list — renderer.js:1513 already honors it, read-only, no trust change. This is a clodex-hand implementation task for tomorrow (I've been hands-on opus today, out of fable tokens).
