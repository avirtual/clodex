---
id: mem-1785111454301-c2vhqq
scope: clodex
learned_at: 2026-07-27T00:17:34.301Z
source: clodex
tags: context-economics
tags_v: 1
pinned: true
---

MCP DECISION (t44, clodex-hand, 2026-07-27; artifact tasks/mcp-decision/journal.md ~350 lines on branch mcp-decision, evidence tagged [CODE]/[CLI]/[DOC]). AWAITING BOGDAN on one question only.

RECOMMENDATION: ship (b) read-only visibility · REFUSE (d) full editing · DEFER (c) per-session enable/disable.

TRUE PREMISE (correcting Bogdan's "we do nothing about mcps" AND my own "42 hits"): 31 tracked hits in 7 files — my 42 wrongly counted vendor/wirescope/ (36, i.e. the WIRE handles MCP, not Clodex) and package-lock.json. Clodex has exactly ONE MCP concept: the `disableClaudeDesignMcp` boolean. MCP exists in our code ONLY as something we SUPPRESS for cost, never as something a user can see or control.

SILENT-DISABLE: QUALIFIED YES, NOT A LIVE DEFECT — I verified this myself. Setting is OFF BY DEFAULT and renderer/index.html:925 spells out the all-or-nothing consequence "including project/user ones". Four paths in; two unsignposted: (3) routed to a pre-v0.6.13 wire advertising no strip_mcp, (4) routed but wire momentarily DOWN at spawn (probe throws). session-manager.js:1045-1049 anticipates path 4 in prose ("A comment, not a code path") — defensible, fail-open beats blocking a spawn. MY ADDITION the hand did not name: the hint says routed sessions need do "nothing" and rests entirely on "strip-capable", a distinction THE USER CANNOT OBSERVE — so paths 3/4 are routed users told they are safe. Dispatched as t45 (observability + hint fix), NOT a behaviour change.

§4 REFRAMES EVERYTHING — per-MCP-server per-turn cost is ALREADY on the wire, parsed, and RENDERED. _tool_roster (status.py:523-547) emits per_tool:[{name,schema_chars,est_tokens}], ships in /_context, crosses IPC as proxy:context, drawn at context-popover.js:247-252; with utilization=1 it carries per-tool `used` counts feeding the existing "Trim N unused tools" lever. MCP tools are PREFIX-KEYED mcp__<server>__* (transforms.py:1185). So "what do my MCP servers cost me" is a CLIENT-SIDE GROUPING of a payload the renderer already receives. "Surface MCP" sounds like config; the evidence says the valuable version is TELEMETRY in a surface we already built.

THE INVARIANT WORTH MORE THAN THE FEATURE (stronger than my own prior): Clodex has NEVER ONCE written into a config file the user also edits. Whole strategy is session-scoped injection at spawn — --agents, --plugin-dir, --settings, all pointing at generated disposable artifacts under ~/.clodex (stores.js:1208: "Writes no file, touches no repo"). Editing MCP would be the FIRST persistent write outside our own directory, and .mcp.json is commonly COMMITTED, so Clodex would generate diffs in the user's working tree.

DATA/CODE: (d) rejected outright — renderer-authored MCP command IS "a GUI-editable command line the app later executes". The rule locates danger at AUTHORSHIP, so: read fine · select-a-name is DATA (same shape as PEER_CLOUD_KINDS) · author command/args/env rejected. TRAP IN (c): per-session disable via --mcp-config + --strict-mcp-config forces Clodex to READ the user's config and RE-EMIT command lines into a file it writes — copy-through, weaker than the tunnel case but depends on an UNVERIFIED scope merge; copying the wrong scope's definition changes WHICH EXECUTABLE RUNS. A code-execution bug wearing a config bug's clothes. Also: MCP configs carry -e API_KEY= and Authorization: headers, so any raw-config display must NOT render env/headers (the roster path is immune by construction — schemas only).

TWO UNVERIFIED, STATED NOT GUESSED: (1) scope precedence on a name collision — not in mcp/add/add-json/list --help, unobservable with zero servers on this box; settling it needs a write to the dev's real config. An unpriced cost on EVERY file-reading tier; the roster is post-resolution truth and never needs the rule. (2) the mcp__<server>__ prefix on a LIVE roster — derived from transforms.py's contract, not observed on real bytes. It is the load-bearing assumption of the whole §4 argument; any implementation MUST confirm it first.

ALSO [CLI]: `claude mcp list/get` distinguish "Pending approval and not connected" — a file-parsing display would report servers that are not there; list also health-checks, so refresh is not free. `--strict-mcp-config` = "Only use MCP servers from --mcp-config, ignoring all other" and we pass it with NO --mcp-config, i.e. the empty set, the flag's maximal form. Codex: ~/.codex/config.toml, one flat user-global file, NO strict equivalent — this is Claude-only.

THE ONE QUESTION FOR BOGDAN (the single input that moves the hand from (b) toward (c)): DO REAL USERS HAVE REAL MCP SERVERS AND HIT THE UNROUTED CASE? Because (b) requires a live routed session — it helps the users who least need help and does NOTHING for the exposed one (unrouted, setting on, real servers). Not a research project; one answer.
