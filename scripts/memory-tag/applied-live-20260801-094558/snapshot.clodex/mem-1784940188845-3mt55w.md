---
id: mem-1784940188845-3mt55w
scope: clodex-brain
learned_at: 2026-07-25T00:43:08.845Z
source: clodex
tags: gorge-once-pattern,wirescope
tags_v: 1
---

OPEN ITEMS after the 2026-07-25 brain session (none blocking, all cheap):
1. `vendor/wirescope` v0.6.40 bump is UNCOMMITTED in the repo (3 files, +35/-8, suite 2218/2218 green). Bogdan's call whether to commit. It is billing-tables-only (verified: git diff v0.6.39..v0.6.40 = CHANGELOG.md + proxylab/billing.py, zero streaming-path files).
2. The winning plan lives at ~/clodex-brain/plugin-plan.fable-5.max.v2.md — OUTSIDE the repo, unversioned. Should be copied somewhere durable. All artifacts + corpus.v1/v2 are backed up in ~/clodex-brain/archive/.
3. Before building from that plan, two reviewer MUST-FIX items: (a) map workbench's dropdown to listWorkspace(rhost.workspaceId), NOT listAll() — ipc-handlers.js:377 is workspace-scoped and the plan widened it at the one site that migrates; (b) restore a renderer teardown hook (deactivate() or rhost.onDispose(fn)) — host-driven disposal cannot remove a plugin's setInterval/addEventListener/ResizeObserver.
4. THREE new shared blind spots for the next curation round: no repaint trigger for status-bar contributions (every renderProxyBar caller is telemetry/switch-driven); the gear menu is type-gated upstream of both APIs (renderer.js:2963-2980, claude/codex only); and grant lifecycle across plugin enable/disable (intentsAllowlistFromChecked keeps only live-catalog types, so disabling a plugin silently deletes its persisted grants).
5. NEW PATTERN CONSTRAINT: usable corpus size is bounded by (window - corpus - output), NOT the window. The tool_result round-trip re-sends corpus + artifact. corpus.v2 (477KB) hit "Prompt is too long" on the round-trip AFTER the artifact was written — failure was cheaper than success, since the round-trip is pure waste for this pattern. Idea parked with wirescope: detect the completed Write tool_use and answer the round-trip locally with a synthetic message_stop, opt-in and recorded as synthetic in the receipt.
6. UNANSWERED: does the client stream-idle clock reset on input_json_delta (tool-arg) deltas, or only text/thinking? If not, the idle ceiling scales with OUTPUT size and a long Write payload is itself a stall risk. Harness default raised to 30 min as insurance.
7. UNTESTED and cheap: fable at HIGH effort on corpus v2 would isolate effort from corpus (tonight's pairwise confounded both). Also untested: opus-5 on corpus v2 — it was the more disciplined reader and its weakness was DEFERRING on questions the missing files would have answered, so the three-way ranking may not survive good inputs.
