---
id: mem-1785107618071-neqwg1
scope: clodex
learned_at: 2026-07-26T23:13:38.071Z
source: clodex
tags: connection-unification,plugin-architecture
tags_v: 1
pinned: true
---

BACKLOG — DO AFTER UNIFICATION (Bogdan's ordering, 2026-07-27):

t31 — DESKTOP WITH ZERO PLUGINS CANNOT REACH MANAGE PLUGINS. The entry point only appears once at least one plugin exists. This is the last gap between "plugins are documented" and "a random user can actually install one": a user who reads the shipped docs, drops a plugin in ~/.clodex/plugins/, and wants to enable it can find the folder but not the manager. Small fix. Bogdan's instruction: handle it AFTER the unification work, not alongside it.

Plugin docs status as of v4.3.2 — DONE, nothing outstanding: plugins/README.md is the launchpad (two halves, ~/.clodex/plugins/, kill switch + quarantine + no sandbox, points at docs/plugin-api.md as the contract rather than restating it); main README names plugins in the feature tour with the link; docs/ ships in the DMG (19 files verified in the built asar, was zero); three wrong passages in plugin-api.md fixed (it described a single in-app plugin dir and claimed a plugin added to an installed copy would not be discovered — denied the BYO feature, untrue since multi-root discovery); git-branches/README.md leads with the user path + Re-scan-without-restart.

Other queued backlog, lower priority: t3 (quit confirmation — reuse the restart busy-check on before-quit), t35 (fold the fourth tokenGated reader at renderer/peers-ui.js:1095 to strict equality — inert, ride it at the head of another pass, do NOT dedicate a dispatch).
