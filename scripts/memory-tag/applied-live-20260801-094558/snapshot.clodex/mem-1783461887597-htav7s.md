---
id: mem-1783461887597-htav7s
scope: clodex
learned_at: 2026-07-07T22:04:47.597Z
source: clodex
tags: wirescope
tags_v: 1
---

Wirescope integration facts: vendored via scripts/vendor-wirescope.sh from ~/projects/proxy-lab into vendor/wirescope; WirescopeSupervisor runs a venv under userData, autostarts when proxy is localhost + port matches; system python3 required (never bundle a runtime). Launch version-check restarts a managed survivor whose /_identity version ≠ vendored RELEASE (once per launch; user-dir sources + adopted externals exempt — a stale wirescopeDir override silently pins old versions and there's no UI to clear it). Stealth UI: one Traffic-optimization toggle; proxyEnabled defaults true. Autostart gap: a mid-run wirescope death isn't respawned until next launch/prefs-save. Strip: per-session stripLevel 0/1/2 asserted by the poller; sticky cold-gated latch; wire-strip trims each request, bake /_compact rewrites disk once — different layers; flipping strip on a warm cache = one-time premium rewrite ("flip once on purpose, never casually"). v0.6.25 PIN_SETTLED_BREAKPOINT pins the penultimate real-user boundary (rescued the 18k-read/127k-write resume collapse). v0.6.26 rider latch: riders fire full-range deterministically once latched; rider_gate is a TOP-LEVEL key in request.json capture records, present ONLY on L2-strip requests (reasons: rider_latched, rider_latched_neutral, rider_cold_latch, no_strip_latch, rider_warmth_unknown, rider_warm_no_latch); latch persists in warmth.sqlite. SIDECALL_MODEL (default on → sonnet) rewrites WebFetch/WebSearch side-calls off the main model — child-row models converge, sub costs shrink; kill switch SIDECALL_MODEL=off in GUI env. Bust chip: LOUD only on fault=content minus per-class restart_between (deploy tax renders calm); fault/fix_hint come off the wire, never re-derived. cost_by_line capability lives on /_identity NOT /_status. /_prune: tier=receipts keeps billing receipts, older_than required (else 400), skipped.recent protects active/warm.
