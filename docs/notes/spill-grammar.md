# spill-grammar.js

## FILED_SRC

The live `[clodex] …` receipt (`spillAckLine`, session-manager.js) is NOT a
shape this grammar matches and must stay outside it: it opens with "body you
typed — was removed from your retained transcript" because the agent's own
history otherwise shows an announcement followed by a confirmation that appeared
on its own — measured on the lead seat 2026-09-23: six phantom dispatches (t1099
×1, t1100 ×3, t1102 ×3) announced a ticket, bound a reminder to it, and typed no
`[agent:task add]` line at all.
