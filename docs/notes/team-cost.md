# team-cost.js

## reviewCostRecord

`model` is NOT behind the measured()/resolved gate its cost neighbours use.
`resolved: false` says the LEDGER could not be summed, which is not a statement
about which model billed, so nulling a model the wire watched bill would drop
the one field that separates a template's effect from its model's in the A/B.
